type Cards {
  civil: String
  uncivil: String
}

enum RoundStep {
  new
  card
  complete
}

enum RoundChoice {
  civil
  uncivil
}

type Round {
  user: UUID!
  step: RoundStep!
  cards: Cards
}

extend type Mutation {
  readyForRound(boxID: UUID!, userUUID: UUID!): Boolean!
  confirmBoardRound(boxID: UUID!, userUUID: UUID!): Boolean!
  setCardRound(boxID: UUID!, userUUID: UUID!, choice: RoundChoice): Boolean!
}
