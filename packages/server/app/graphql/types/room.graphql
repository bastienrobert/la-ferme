type ConnectedUsers {
  boxID: UUID!
  users: [User]!
}

extend type Query {
  connectedUsers(boxID: UUID!): ConnectedUsers!
}

extend type Mutation {
  joinRoom(boxID: UUID!, userUUID: UUID!): UUID!
}

extend type Subscription {
  connectedUsers(boxID: UUID!): ConnectedUsers!
}
