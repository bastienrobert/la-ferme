extend type Query {
  rooms: [Room]
}

extend type Mutation {
  join: Float
  roundComplete: Float
}

extend type Subscription {
  roundCompleted: Float
  connection: Float
}

type Room {
  id: Int
}
