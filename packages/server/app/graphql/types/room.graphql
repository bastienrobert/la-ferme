extend type Mutation {
  joinRoom(box_id: UUID!, user_uuid: UUID!): UUID!
}

type ConnectedUser {
  user_uuid: UUID!
}

type ConnectedUsers {
  box_id: UUID!
  connectedUsers: [ConnectedUser]
}

extend type Subscription {
  connectedUsers(box_id: UUID!): ConnectedUsers
}
