type Room {
  boxID: UUID!
}

type ConnectedUsers {
  boxID: UUID!
  creatorUUID: UUID!
  users: [User]!
}

extend type Mutation {
  joinRoom(boxID: UUID!, userUUID: UUID!): ConnectedUsers!
}

extend type Subscription {
  connectedUsers(boxID: UUID!): ConnectedUsers!
}
