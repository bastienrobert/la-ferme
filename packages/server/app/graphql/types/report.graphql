type Report {
  boxID: UUID!
  from: Player!
  to: Player!
}

extend type Mutation {
  reportPlayer(boxID: UUID!, fromUUID: UUID!, toUUID: UUID!): Boolean!
}

extend type Subscription {
  playerIsReport(boxID: UUID!): Report!
}
